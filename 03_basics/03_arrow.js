const user={
    name:'John',
   welcomeMessage:function(){
        console.log(`Welcome back ${this.name}`);
    }
}
user.welcomeMessage(); //Welcome back John

// Now if we want to use arrow function then we can do it like this
};  

